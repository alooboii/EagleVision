# Model Configs

Model and adapter settings live here.

## Current Files

- `depth_adapter.yaml`: pretrained depth model plus adapter settings.
- `roundtrip.yaml`: round-trip depth/NVS model settings.

Use these configs for reusable architecture choices. Runtime choices such as output directory, step count, and evaluation limits belong in `train/` or `eval/`.
