# Outputs

This folder is for local generated outputs.

The repository tracks this README and `.gitkeep` only. Generated panels, predictions, checkpoints, and temporary run artifacts should stay ignored by git.

Use subfolders such as:

```text
outputs/baseline/
outputs/train/
outputs/eval/
outputs/demo/
```

Promote only small curated summaries into `results/` or documentation.
